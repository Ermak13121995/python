
a = int(input('Введите натуральное число: '))
total = 0

while a % 2 == 0:
    # Проверяем, делится ли число нацело
    total += 1
    # Если делится, то прибавляем 1 к переменной total
    a = a / 2
    # делим первоначальное число на 2, чтобы цикл проверял следующее число

print('Делится нацело', total, 'раз(а)')
