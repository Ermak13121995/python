s = input(' Введите ваш IP: ')
ip = s.split('.')
#разделяем числа по делителю .
flag = 0
# флажок, для определения диапазона числа

for i in range(len(ip)):
    # перебираем строку ip
    if 0 <= int(ip[i]) <= 255:
        # проверяем числа на соответствие диапазону
        flag += 1
    else:
        print('NO')
        # Если хоть одно число не соответствует, то прерываем цикл
        break

if flag == 4:
    print('YES')
# если все числа соответствуют диапазону, выводим yes
