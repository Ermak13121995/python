a = int(input('Введите число: '))
total = 0


while a != 0:
    # Ставим условие для проверки числа, пока оно не станет равно 0
    last_digit = a % 10
    # Берём последнюю цифру числа
    if last_digit == 5:
        # Задаём условие
        total += 1
        # Если последняя цифра числа равно 5, то в total записываем +1
    a = a // 10
    # После одной иттерации удаляем последнюю цифру числа и переходим к следующей

print('Количество цифр 5 в числе равно: ', total)
